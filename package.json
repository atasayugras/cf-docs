{
  "scripts": {
    "docs:fetch": "bun run scripts/fetch-external-docs.ts",
    "docs:dev": "bun run docs:fetch && vitepress dev docs",
    "docs:build": "bun run docs:fetch && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "vitepress": "^2.0.0-alpha.17"
  }
}